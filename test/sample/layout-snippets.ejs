<%# 复制下面一行到主题 layout 对应页面（如 tag.ejs / index.ejs）的空白处 %>
<%# 若报错「forcegraph 未定义」：请在博客根目录 npm install hexo-tag-force-graph --save，且 _config.yml 的 plugins 中加入 - hexo-tag-force-graph %>
<%- forcegraph('500px', '#111') %>
<%# 可选：<%- forcegraph('600px') %> 或 <%- forcegraph('500px', '#1a1a2e') %> %>
